# 「面评家」—— AI 多轮结构化面试 Agent · 技术方案

> **赛题**:赛题2「AI+软件创新」
> **对象**:本科计科团队(2–4 人,开发周期 6 周)
> **核心主张**:不是"套壳问答机器人",而是一个**带状态机、能动态追问、输出标准化评估报告**的 AI 面试官。

---

## 一、项目概述

### 1.1 一句话定位
输入一份 **岗位 JD + 一批简历**,AI 作为面试官完成**首轮结构化面试**(多轮动态追问),输出每位候选人的**标准化评估报告**。

### 1.2 目标用户与商业价值
| 用户 | 场景 | 付费意愿 |
|---|---|---|
| 中小公司 HR | 校招/实习/初级岗海量初筛 | 高(省 HR 时间) |
| 招聘平台 | 为平台用户提供 AI 首面增值服务 | 高(B2B2C) |
| 校招办/就业指导中心 | 模拟面试训练 | 中(可走采购) |

商业模式:SaaS 按次计费(¥1–3/次)或订阅;对学校就业指导中心开放免费试用 + 合作试点。

### 1.3 与"聊天机器人"的本质区别(核心卖点)
| | 普通聊天机器人 | 面评家 |
|---|---|---|
| 提问方式 | 一次性/随机 | **按 JD 能力维度 + STAR 法则动态追问** |
| 会话状态 | 无状态 | **状态机驱动**:维度推进、追问轮次、防跑题 |
| 评估 | 无 | 结构化评分卡 + 证据引用 + 录用建议 |
| 标准化 | 无法比较 | 同岗位同一套考察框架,可横向对比 |

---

## 二、业务需求分析

### 2.1 用户故事(优先级排序)
- **P0(必须有)**:
  - 作为 HR,上传 JD 和简历,系统自动生成**能力考察大纲**;
  - 作为 HR,发起一场文字面试,候选人通过链接/小程序参加;
  - 作为候选人,被 AI 逐维度追问,感受到"像真人面试官";
  - 作为 HR,面试结束查看**结构化评估报告**(维度得分 + 证据 + 建议)。
- **P1(应有)**:
  - 语音面试(ASR 转写);
  - 候选人端进度提示(还剩几个维度);
  - 同批候选人**横向对比表**。
- **P2(可以有)**:
  - 面试回放(逐轮日志);
  - 简历→JD 差距热力图。

### 2.2 功能需求清单
| 模块 | 功能 |
|---|---|
| 简历解析 | PDF/DOCX → 结构化字段(教育/经历/技能/项目) |
| JD 分析 | JD → 能力维度清单(硬技能/软素质) |
| 差距分析 | 简历 vs JD → 考察重点与缺口 |
| 面试会话 | 多轮动态追问、维度推进、防跑题 |
| 评估报告 | 维度打分、雷达图、录用建议、证据引用 |
| 管理后台 | JD/候选人/面试记录/报告管理 |

---

## 三、系统总体架构

```
┌──────────────────────────── 前端 ────────────────────────────┐
│  HR 端(Web 后台):JD 管理 | 简历上传 | 发起面试 | 查看报告      │
│  候选人端(网页/小程序):面试页面(文字/语音)+ 进度提示           │
└───────────────▲─────────────────────────────────────────┘
                │ REST API + WebSocket
┌───────────────┴─────────────────────────────────────────┐
│                   服务层(FastAPI)                         │
│  /api/jobs  /api/candidates  /api/interviews             │
│  /api/interviews/{id}/message  /api/interviews/{id}/eval │
└───────────────▲─────────────────────────────────────────┘
                │
┌───────────────┴─────────────────────────────────────────┐
│           Agent 编排层(LangGraph 状态机)                  │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐ │
│  │ 简历解析 Agent│ → │ JD 分析 Agent│ → │ 差距分析 Agent│ │
│  └──────────────┘   └──────────────┘   └──────────────┘ │
│                     ┌──────────────┐   ┌──────────────┐ │
│                     │ 面试官 Agent  │ → │ 评估 Agent   │ │
│                     │ (多轮状态机)  │   │              │ │
│                     └──────────────┘   └──────────────┘ │
└───────────────▲─────────────────────────────────────────┘
                │
┌───────────────┴─────────────────────────────────────────┐
│  能力层/工具                                               │
│  ASR 语音转写 | 文档解析(pdfplumber/python-docx) | 规则引擎 │
└───────────────▲─────────────────────────────────────────┘
                │
┌───────────────┴─────────────────────────────────────────┐
│  数据层                                                   │
│  PostgreSQL(候选人/JD/面试/消息/评分)                      │
│  向量库 Qdrant(能力维度模板/优秀回答样本)                   │
│  LLM API(DeepSeek-V3 / 通义千问,Function Calling)        │
└─────────────────────────────────────────────────────────┘
```

---

## 四、核心模块详细设计

### 4.1 模块①简历解析 Agent
- **输入**:PDF / DOCX / 文本简历;
- **处理**:`文档解析 → 清洗 → LLM 结构化抽取(JSON Schema 约束输出)`;
- **输出**:
  ```json
  {
    "basic": {"name": "", "age": "", "school": "", "major": ""},
    "education": [{"school": "", "degree": "", "major": "", "years": ""}],
    "experience": [{"company": "", "role": "", "duration": "", "summary": ""}],
    "projects": [{"name": "", "tech_stack": [], "achievements": ""}],
    "skills": {"programming": [], "tools": [], "soft": []}
  }
  ```
- **可靠性**:解析失败或字段缺失 → 返回"缺失项清单",让 HR 手动补录(P0 允许最小程度的人工兜底)。

### 4.2 模块② JD 分析 Agent
- **输入**:JD 文本;
- **处理**:抽取岗位能力维度,分三类:
  - **硬技能**(技术栈、工具,如 Python/MySQL/K8s);
  - **软素质**(沟通、学习能力、抗压、团队协作);
  - **行为要求**(如"能独立负责模块"→ 行为面试考察点)。
- **输出**:
  ```json
  {
    "dimensions": [
      {"name": "Python 编程", "type": "hard", "weight": 0.3, "keywords": ["Python", "Flask", "asyncio"]},
      {"name": "数据库", "type": "hard", "weight": 0.2, "keywords": ["MySQL", "索引", "事务"]},
      {"name": "沟通表达", "type": "soft", "weight": 0.15, "probe": "STAR"},
      {"name": "团队协作", "type": "soft", "weight": 0.15, "probe": "STAR"}
    ],
    "junior_level": true
  }
  ```
- 同一岗位可缓存"考察大纲",供后续多个候选人复用(**标准化基础**)。

### 4.3 模块③差距分析 Agent
- **输入**:结构化简历 + JD 维度清单;
- **处理**:逐维度判断"简历已证明 / 简历存疑 / 完全缺失";
- **输出**:每个维度的**考察策略**:
  - 简历已证明 → 考察深度(追问项目细节、验证真实度);
  - 简历存疑 → 重点考察(给出针对性情境题);
  - 完全缺失 → 给"最低门槛"情境题,确认是否可接受;
- 同时生成每个维度的**首问推荐**(开场问题)。

### 4.4 模块④面试官 Agent(核心,详见第五章)
多轮动态追问的**会话状态机**,是整个系统的技术心脏。

### 4.5 模块⑤评估 Agent
- **输入**:完整面试消息流 + 各维度考察记录;
- **处理**:分维度总结证据(引用候选人的**原话片段**)+ 打分;
- **输出评估报告**:
  ```json
  {
    "summary_score": 78,
    "suggestion": "建议进入二面",
    "dimensions": [
      {"name": "Python 编程", "score": 8.0, "evidence": ["候选人提到用 asyncio 优化了 QPS", "..."]},
      {"name": "沟通表达", "score": 6.5, "evidence": ["..."]}
    ],
    "strengths": ["..."],
    "risks": ["简历与回答有出入:..."],
    "next_step_questions": ["二面建议重点考察:..."]
  }
  ```
- **打分口径**:每个维度按「简历基础 + 回答深度 + STAR 完整度 + 沟通质量」加权。

---

## 五、面试官 Agent 与多轮状态机设计(技术心脏)

### 5.1 为什么必须用状态机
一次性提问是"聊天机器人";只有**每轮基于候选人的回答做出结构化决策(追问/推进/收尾)**,才是"AI 面试官"。状态机保证了:
- 覆盖 JD 全部维度,不漏项;
- 追问有上限,不无限纠缠;
- 可回放、可审计(答辩可展示决策过程)。

### 5.2 面试会话数据结构
```python
class InterviewState(TypedDict):
    interview_id: int
    phase: str          # OPENING | PROBING | BEHAVIORAL | CANDIDATE_QA | CLOSING
    dim_idx: int        # 当前考察的第几个维度
    dimensions: list[dict]   # 见 4.2 的 JD 分析输出
    dim_question_count: dict # {dim_name: 已问次数}
    history: list[dict]      # [{"role":"agent"/"candidate","text":...}]
    max_q_per_dim: int = 3   # 每个维度最多 3 问
    max_total_q: int = 15    # 全场最多 15 问
    pending_verification: list  # 面试中发现的待核验点
```

### 5.3 状态转换图
```
         ┌──────────────┐
         │   OPENING    │  (自我介绍+暖场,1-2 轮)
         └──────┬───────┘
                ▼
   ┌────────────┴─────────────┐
   │        PROBING           │  ← 逐维度考察(硬技能)
   │  dim_idx = 0..N-1        │
   └────────────┬─────────────┘
                │
   每轮:面试官提问 → 候选人回答 → 质量判断
                │
        ┌───────┴────────┐
        │   质量判断      │
        │ answered?      │
        │ quality_score  │
        └───────┬────────┘
    ┌───────────┼───────────────┐
    ▼           ▼               ▼
 未答/跑题   质量不足          质量达标
  追问1次   (追问机会)       (换下一维度)
    │          │               │
    └──────────┴───┬───────────┘
                   ▼
        本维度达到 3 问上限? 否→ 继续 PROBING(下一维)
                   │是
                   ▼
   所有硬技能维度完成 ──>  BEHAVIORAL(STAR 行为面试,软素质)
                   │
                   ▼
         ┌── CANDIDATE_QA(给候选人提问机会,1 轮) ──┐
                   │
                   ▼
                CLOSING(收尾:告知结束→触发评估)
```

### 5.4 每轮"提问-判断"循环(关键算法)
```
输入:上一问 + 候选人回答 + 当前维度 + 历史
输出:结构化决策(见 5.5)

算法:
1. 判断"是否算有效回答":
   - 跑题检测:回答与问题主题语义相似度(embedding)低于阈值 → 记为"回避/跑题";
   - 空话检测:无实质信息(泛泛而谈) → 记为"浅答";
2. 若"回避/跑题":
   - 追问次数 < 2 → 追问同维度同要点,并提示"请围绕刚才的问题回答";
   - 否则 → 记录"该维度未证明",推进到下一维度;
3. 若"浅答":
   - 追问同维度"具体细节"(请举例/数据/过程);
   - 达到 3 问上限 → 记录"深度不足",推进;
4. 若"回答充分":
   - 记录高质量回答 + 证据;
   - 推进到下一维度(或阶段切换);
5. 全场达到 15 问 → 无论进度如何,进入收尾。
```

### 5.5 面试官 Agent 的 LLM 输出协议(严格 JSON)
**系统 Prompt 骨架**(重点,决定面试质量):
```
你是[岗位]方向的高级面试官。你正在对候选人进行首轮结构化面试。
【考察大纲】(来自 JD 分析)
{维度列表及各维度考察要点}
【面试规则】
1. 一次只问一个问题,不要连续抛多个问题;
2. 基于候选人上一句回答追问细节,而不是跳回开场白;
3. 用 STAR 法则考察行为类维度(情境/任务/行动/结果);
4. 追问次数达到上限时给出过渡语并自然切换到下一维度;
5. 语气专业、平等、不评判;不说教;不泄露评估结论;
6. 每次输出必须符合给定的 JSON 结构。
```

**每轮 LLM 输出 Schema(用 Function Calling 约束)**:
```json
{
  "thinking": "内部推理,不展示给候选人;判断回答质量与下一步",
  "assess": {
    "answered": true,
    "quality": 7,
    "issue": "具体详实",
    "evidence": "候选人提到:用 asyncio 把接口 QPS 从 50 优化到 300"
  },
  "next_question": "你刚提到用 asyncio 优化,具体是遇到了什么瓶颈?当时怎么定位的?",
  "action": "CONTINUE_DIMENSION"
}
```
`action` 枚举:`CONTINUE_DIMENSION | NEXT_DIMENSION | GO_BEHAVIORAL | GO_CANDIDATE_QA | CLOSING`
- 服务端根据 `action` + 轮次上限**再次校验**(LLM 出错时兜底),确保状态机不越界;
- 这样 **LLM 负责"聪明的判断",状态机负责"流程的可靠性"**,两者解耦。

### 5.6 防跑题 / 防作弊设计
- **语义相关性检测**:候选人回答经 embedding 与问题主题比对,过低触发"请围绕问题回答"提示;
- **追问上限**:每维度 ≤3 问、全场 ≤15 问,防无意义纠缠;
- **真实性核验**:面试官 Agent 对"简历与回答不一致"自动记入 `pending_verification`,评估报告会标红提示 HR 复核;
- **定时超时**:候选人 5 分钟未回复 → 自动结束并生成"未完成"报告。

---

## 六、数据模型设计

### 6.1 数据库表(PostgreSQL)
```sql
-- 岗位(JD)
CREATE TABLE jobs (
  id            SERIAL PRIMARY KEY,
  title         TEXT NOT NULL,
  jd_text       TEXT NOT NULL,
  dimensions    JSONB,          -- JD 分析 Agent 输出的维度清单
  created_at    TIMESTAMP DEFAULT now()
);

-- 候选人
CREATE TABLE candidates (
  id            SERIAL PRIMARY KEY,
  name          TEXT,
  resume_path   TEXT,
  resume_text   TEXT,
  parsed_resume JSONB,          -- 简历解析 Agent 输出
  created_at    TIMESTAMP DEFAULT now()
);

-- 面试
CREATE TABLE interviews (
  id            SERIAL PRIMARY KEY,
  job_id        INT REFERENCES jobs(id),
  candidate_id  INT REFERENCES candidates(id),
  status        TEXT DEFAULT 'created',  -- created | probing | behavioral | finished | timeout
  state         JSONB,          -- InterviewState 快照(恢复用)
  report        JSONB,          -- 评估 Agent 输出
  started_at    TIMESTAMP,
  ended_at      TIMESTAMP
);

-- 面试消息(逐轮留痕,审计与回放)
CREATE TABLE interview_messages (
  id            SERIAL PRIMARY KEY,
  interview_id  INT REFERENCES interviews(id),
  role          TEXT,           -- agent | candidate
  text          TEXT,
  dimension     TEXT,           -- 属于哪个维度
  assess        JSONB,          -- 面试官该轮的 assess 输出(质量分/证据)
  created_at    TIMESTAMP DEFAULT now()
);
```

### 6.2 状态持久化
- 每轮结束将 `InterviewState` 写入 `interviews.state`(JSONB);
- 候选人或 HR 刷新页面,可从快照恢复会话;
- 这同时是**回放功能的底层数据**,答辩时能逐轮展示决策过程。

---

## 七、API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/jobs` | 创建岗位,触发 JD 分析 |
| POST | `/api/candidates` | 上传简历(PDF/DOCX),触发简历解析 |
| POST | `/api/interviews` | 发起面试(绑定 job+candidate),返回面试 ID 与候选人链接 |
| POST | `/api/interviews/{id}/message` | 候选人提交回答,返回面试官下一问 + 会话状态 |
| GET | `/api/interviews/{id}/messages` | 拉取历史消息 |
| GET | `/api/interviews/{id}/state` | 拉取当前会话状态(进度/剩余维度) |
| POST | `/api/interviews/{id}/evaluate` | 触发评估(收尾后自动调用) |
| GET | `/api/interviews/{id}/report` | 获取评估报告 |
| GET | `/api/candidates/{id}/report` | 候选人与 JD 的差距热力图(可选) |

**候选人链接**:`POST /api/interviews` 返回 `https://<host>/interview/{id}`,候选人免登录进入(匿名安全 token)。

---

## 八、技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Python + FastAPI + Uvicorn | 生态全、上手快、自带 Swagger 文档 |
| Agent 编排 | **LangGraph**(StateGraph) | 状态机原生支持、可回放,答辩可展示图结构 |
| LLM API | DeepSeek-V3 / 通义千问 / 智谱(GLM) | 国产、便宜、支持 Function Calling |
| 结构化输出 | Function Calling / `response_format=json` | 强制 JSON,保证状态机能解析 |
| 向量库 | Qdrant | embedding 相关性检测、优秀回答样本检索 |
| 语音(可选) | 国产 ASR API(腾讯/百度免费额度) | 语音面试模式 |
| 文档解析 | pdfplumber + python-docx | 简历 PDF/DOCX |
| 前端 | 候选人页 Vue3;后台 Vue3 + Element Plus | 成品感强 |
| 部署 | Docker Compose + 服务器 | 现场可跑 |

---

## 九、开发里程碑(6 周,2–4 人)

### 分工建议
- **A(1 人)**:简历/JD 解析 + 数据模型 + API;
- **B(1 人)**:面试状态机 + 面试官 Agent(核心,投入最多);
- **C(1 人)**:评估 Agent + 报告页;
- **D(1 人,可兼职)**:前端候选人页 + 管理后台 + 部署。

### 里程碑表
| 周次 | 目标 | 验收标准 |
|---|---|---|
| W1 | 数据模型 + 简历解析 + JD 分析 | 上传 10 份样例简历→全部结构化;JD→维度清单 |
| W2 | **面试状态机 v1**(单维度问答闭环) | 候选人能回答,面试官能基于回答出下一问 |
| W3 | **多维度推进 + 追问策略 + 跑题检测** | 一场面试自动覆盖全部维度,不越上限 |
| W4 | 评估 Agent + 评估报告页 | 面试结束→自动出报告(雷达图+证据+建议) |
| W5 | 候选人前端 + 管理后台 + 语音(可选) | 完整闭环:HR 发起→候选人答题→HR 看报告 |
| W6 | 打磨 + 演示数据 + 部署 + 答辩材料 | 现场可跑完整演示 |

### 关键验收指标(写入 README,答辩用)
- 面试轮数:单场 8–15 问(维度全覆盖);
- 追问触发率:≥60% 的轮次发生动态追问(证明"多轮"而非"问答机");
- 评估一致率:同一份简历+固定回答跑 3 次,各维度得分极差 ≤1 分(证明稳定性);
- 单场成本:API 费用 < ¥1/场(证明商业上可行)。

---

## 十、测试与评估设计

### 10.1 Agent 级测试(必做,拿分项)
- **状态机冒烟测试**:脚本模拟候选人,输入预设回答序列(如"一直跑题""全部答满""中途不答"),断言状态转换符合预期、不越轮次上限;
- **Schema 校验测试**:LLM 输出必须通过 JSON Schema 校验,失败自动重试(最多 2 次);
- **回归数据集**:准备 20 份简历 + 固定答案脚本,每次改动后跑一遍,防止面试质量回归。

### 10.2 面试质量评估(答辩可展示)
- 让 2–3 名同学分别体验面评家,与真人面试对比打分:
  - 提问相关性(是否围绕能力);
  - 追问质量(是否越问越深);
  - 流畅度(是否尴尬/说教/重复);
- 用问卷收集打分,写进答辩 PPT 作"用户验证"证据。

---

## 十一、部署与演示方案

### 11.1 部署
```bash
docker-compose up          # 前端 + 后端 + PostgreSQL + Qdrant 一键起
python scripts/make_demo.py  # 生成演示数据(5 个岗位 + 20 份简历 + 预置面试)
```

### 11.2 现场演示脚本(5 分钟)
1. **30s**:HR 端上传 JD + 简历,展示自动生成的"考察大纲"与"差距热力图";
2. **2min**:现场随机挑一名评委当"候选人",开一场文字面试——展示面试官**基于回答动态追问**(抛一个泛泛回答,面试官追问细节,直观体现"多轮");
3. **1min**:人工快速结束面试 → 展示评估报告(维度雷达图 + 证据原话引用 + 录用建议);
4. **1min**:展示管理后台"同批候选人横向对比表";
5. **30s**:讲清三组指标(追问率、评估一致率、单场成本 <¥1)。

### 11.3 答辩技术叙事(30 秒版)
> "市面上的 AI 面试工具大多是'一次性提问的聊天机器人'。我们做的是**带状态机的 AI 面试官**:先把 JD 拆成能力维度,再对每个维度动态追问,LLM 负责判断、状态机负责兜底流程,最后输出带证据引用的标准化评估报告。同一岗位、同一套考察框架,让 HR 能横向比较候选人了。"

---

## 十二、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| LLM 输出解析失败 | 状态机卡死 | Function Calling 强制 JSON + Schema 校验 + 失败重试(≤2 次)+ 兜底默认问题 |
| 面试质量不稳定 | 体验差 | 高质量系统 Prompt + 维度模板沉淀到 Qdrant + 固定答案回归测试 |
| 候选人跑题/刷题 | 评估失真 | 跑题检测 + 追问上限 + `pending_verification` 人工复核标记 |
| 简历与回答不一致 | 误判 | 面试官主动核验简历细节,不一致项进报告"风险区" |
| "已有竞品"质疑 | 答辩失分 | 主打差异化:状态机驱动 + 动态追问 + 证据引用 + 同批横向对比 |
| 语音模式成本 | 单场成本上升 | 语音做 P1(可选),MVP 先文字;ASR 用免费额度 |

---

## 十三、MVP 边界与后续迭代

### 13.1 MVP 范围(6 周内必须做完)
- 文字面试完整闭环 + 评估报告 + 管理后台 + 演示数据;
- 语音、横向对比表、差距热力图、面试回放**均为 P1 加分项**,时间不够就砍。

### 13.2 后续迭代方向(答辩"未来规划")
- 多 Agent 评委团(技术面 + HR 面分角色);
- 面试题目库沉淀 + 难度自适应(根据回答自动调深/调浅);
- 接入视频面试(音视频 SDK)与情绪/语音特征分析;
- 与招聘系统(ATS)打通,简历数据回流。

---

## 十四、交付物清单
- 后端代码(FastAPI + LangGraph 状态机),含测试;
- 前端:候选人面试页 + HR 管理后台;
- `README.md`:架构说明 + 部署方式 + 验收指标;
- `scripts/make_demo.py`:一键生成演示数据;
- 演示脚本 + 答辩 PPT(含用户验证问卷结果)。

> **一句话总结**:面评家的技术含金量 = **"JD 拆解 → 维度化动态追问 → 结构化评估"的完整 Agent 闭环**,工程上完全在本科 2–4 人团队的能力范围内,而"带状态机的 AI 面试官"这个叙事,足以在赛题2打动评委。
